# Routes

V1: `/` only.

Use anchor navigation: `#fruit-chaat`, `#chips-adda`, `#gallery`, `#visit` (only if visit information exists). Do not create empty routes merely to make the site seem larger.
