# Prompt 09 — QA
Run typecheck, lint and production build. Test keyboard navigation, focus visibility, reduced motion, image dimensions, broken links, console errors, semantic headings, contrast, responsive overflow and major browsers where available. Verify data-driven menu rendering. Fix failures rather than merely reporting them.
