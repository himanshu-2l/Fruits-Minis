# Performance

Target a fast mobile storefront. Optimize and appropriately size images, lazy-load below fold, preload only truly critical assets, minimize client components, avoid animation libraries where CSS suffices, keep third-party scripts minimal, prevent layout shift, and audit Lighthouse after implementation. Do not embed heavy social feeds by default; use curated local assets or lightweight links/previews.
